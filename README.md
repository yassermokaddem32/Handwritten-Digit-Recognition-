# Handwritten-Digit-Recognition-
A Python-based application that recognizes handwritten digits using a trained deep learning model. It processes input images and predicts digits (0–9), demonstrating core concepts of image classification and neural networks.
